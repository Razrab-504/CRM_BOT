# 🤖 Telegram CRM Bot

Полнофункциональная CRM-система для управления заказами между клиентами и исполнителями в Telegram.

## 📋 Описание

Telegram-бот для автоматизации бизнес-процессов: поиск исполнителей, создание заказов, система рейтингов и отзывов. Подходит для фриланс-платформ, сервисных центров, маркетплейсов услуг.

### ✨ Основные возможности

- 👥 **Двухсторонняя регистрация** - клиенты и исполнители
- 🔍 **Умный поиск** - фильтрация по специализациям и рейтингу
- 📝 **Управление заказами** - полный цикл от создания до завершения
- ⭐ **Система рейтингов** - отзывы и оценки исполнителей
- 📊 **Статистика** - аналитика для исполнителей
- 🔔 **Уведомления** - автоматические оповещения о статусах заказов
- 🗄️ **Асинхронная БД** - быстрая работа с данными

## 🎯 Категории услуг

- 💻 IT (разработка, администрирование)
- 🎬 Видеомонтаж
- 🏋️ Тренеры (фитнес, спорт)
- 📚 Учителя (репетиторство)
- 🎨 Дизайн (графика, UX/UI)

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- PostgreSQL
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/yourusername/telegram-crm-bot.git
cd telegram-crm-bot
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте базу данных**
```bash
mysql -u root -p
```

```sql
CREATE DATABASE crm_bot CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'bot_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON crm_bot.* TO 'bot_user'@'localhost';
FLUSH PRIVILEGES;
```

5. **Создайте файл `.env`**
```bash
cp .env.example .env
```

Заполните `.env`:
```env
BOT_TOKEN=your_bot_token_here
DB_HOST=localhost
DB_PORT=3306
DB_USER=bot_user
DB_PASSWORD=your_password
DB_NAME=crm_bot
```

6. **Примените миграции**
```bash
python src/create_database.py
```

7. **Запустите бота**
```bash
python main.py
```

## 📁 Структура проекта

```
CRM_BOT/
├── main.py                    # Точка входа
├── config.py                  # Конфигурация
├── requirements.txt           # Зависимости
├── .env                       # Переменные окружения
├── README.md                  # Документация
├── .gitignore                # Git ignore файл
│
├── src/
│   ├── bot/
│   │   ├── handlers/         # Обработчики команд
│   │   │   └── user/
│   │   │       └── user_handlres.py  # Логика пользователя
│   │   │
│   │   └── kbd/              # Клавиатуры
│   │       ├── admin_keyboard.py     # Админ кнопки
│   │       └── user_keyboard.py      # Пользовательские кнопки
│   │
│   ├── db/
│   │   ├── crud/             # CRUD операции
│   │   │   ├── __init__.py
│   │   │   ├── client.py    # Операции с клиентами
│   │   │   ├── employee.py  # Операции с исполнителями
│   │   │   ├── order.py     # Операции с заказами
│   │   │   └── review.py    # Операции с отзывами
│   │   │
│   │   └── models/           # SQLAlchemy модели
│   │       ├── __init__.py
│   │       ├── client.py    # Модель клиента
│   │       ├── employee.py  # Модель исполнителя
│   │       ├── order.py     # Модель заказа
│   │       └── review.py    # Модель отзыва
│   │
│   ├── base.py               # Base для моделей
│   ├── config.py             # Конфигурация проекта
│   ├── create_database.py    # Создание БД
│   ├── enums.py              # Enum классы (статусы, категории)
│   └── session.py            # Сессия БД
│
└── venv/                     # Виртуальное окружение
```

## 🗄️ Структура базы данных

### Таблица `employee` (Исполнители)
```sql
- id (PK)
- telegram_user_id (UNIQUE)
- first_name
- last_name
- phone
- birth_date
- branch (ENUM: IT, VIDEO_EDITING, TRAINER, TEACHER, DESIGN)
- rating (0.00 - 5.00)
- total_reviews
- created_at
```

### Таблица `clients` (Клиенты)
```sql
- id (PK)
- telegram_user_id (UNIQUE)
- first_name
- last_name
- phone
- birth_date
- created_at
```

### Таблица `orders` (Заказы)
```sql
- id (PK)
- client_id (FK → clients)
- employee_id (FK → employee)
- description
- price
- status (ENUM: PENDING, IN_PROGRESS, COMPLETED, CANCELLED)
- created_at
- finished_at
```

### Таблица `reviews` (Отзывы)
```sql
- id (PK)
- client_id (FK → clients)
- employee_id (FK → employee)
- order_id (FK → orders)
- rating (1-5)
- comment
- created_at
```

## 🎮 Использование

### Для клиента:
1. `/start` → Выбрать "Я клиент"
2. Заполнить данные регистрации
3. "🔍 Найти исполнителя" → Выбрать категорию
4. Выбрать исполнителя → Описать задачу
5. Дождаться подтверждения
6. После завершения работы → "Завершить заказ" → Оставить отзыв

### Для исполнителя:
1. `/start` → Выбрать "Я исполнитель"
2. Заполнить данные + выбрать специализацию
3. Получать уведомления о новых заказах
4. "✅ Принять" или "❌ Отклонить"
5. Связаться с клиентом по телефону
6. После завершения клиент закроет заказ

## 📊 Команды бота

Бот имеет одну команду:
- `/start` - Запуск бота, регистрация и доступ ко всем функциям через интерактивное меню с кнопками

## 🛠️ Технологии

- **[Aiogram 3.x](https://docs.aiogram.dev/)** - Асинхронный фреймворк для Telegram Bot API
- **[SQLAlchemy 2.x](https://www.sqlalchemy.org/)** - ORM для работы с БД
- **[MySQL 8.0+](https://www.mysql.com/)** - Реляционная база данных
- **[Alembic](https://alembic.sqlalchemy.org/)** - Миграции БД
- **[python-dotenv](https://pypi.org/project/python-dotenv/)** - Управление переменными окружения
- **[asyncio](https://docs.python.org/3/library/asyncio.html)** - Асинхронное программирование

## 📦 Зависимости

```txt
aiogram==3.4.1
SQLAlchemy==2.0.25
pymysql==1.1.0
cryptography==41.0.7
python-dotenv==1.0.0
alembic==1.13.1
```

## 🔐 Безопасность

- ✅ Хранение конфиденциальных данных в `.env`
- ✅ Валидация пользовательского ввода
- ✅ Защита от SQL-инъекций (SQLAlchemy ORM)
- ✅ Ограничение доступа к командам (middleware)
- ⚠️ **НЕ коммитьте `.env` в Git!**

## 🚧 Roadmap

- [ ] Админ-панель (веб-интерфейс)
- [ ] Интеграция платежей (Stripe/ЮKassa)
- [ ] Мультиязычность (RU/EN/AZ)
- [ ] Экспорт отчетов (CSV/Excel)
- [ ] AI рекомендации исполнителей
- [ ] Система споров и арбитража

## 🤝 Контрибьюция

Контрибьюции приветствуются! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

## 👨‍💻 Автор

**Ваше Имя**
- Telegram: [@motivator6438](https://t.me/@motivator6438)
- GitHub: [@Razrab-504]((https://github.com/Razrab-504))
- Email: mammedovibrahim38@gmail.com

## 🌟 Поддержка

Если проект оказался полезным, поставьте ⭐ на GitHub!

---

**Made with ❤️ using Python & Aiogram**
